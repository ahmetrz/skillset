name: Final audit validation gate v1 (archived)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Final master is authoritative
        run: |
          echo "Final master validation is complete and authoritative."
          echo "This legacy Turso-based validator is archived to prevent stale counters from overwriting final checkpoints."
