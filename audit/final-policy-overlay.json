{
  "generatedAt": "2026-09-05T10:54:28.099Z",
  "version": 1,
  "approval": "user-approved-all-37-recommendations",
  "selections": {
    "clarification": "proceed, except materially ambiguous/high-risk actions",
    "testing": "first",
    "git_destructive": "forbid",
    "production": "approval",
    "priority": "security",
    "memory": "stateless by default; persistent only when explicitly scoped",
    "branch": "pr",
    "provider": "agnostic",
    "coverage": "avoid one global hard-coded value; choose a context/risk-specific default with an override",
    "max_attempts": "avoid one global hard-coded value; choose a context/risk-specific default with an override",
    "retry": "avoid one global hard-coded value; choose a context/risk-specific default with an override",
    "score": "avoid one global hard-coded value; choose a context/risk-specific default with an override",
    "threshold": "avoid one global hard-coded value; choose a context/risk-specific default with an override",
    "timeout": "avoid one global hard-coded value; choose a context/risk-specific default with an override",
    "tool:ansible": "prefer portable/default tooling; never-ban only when safety or compatibility requires it",
    "tool:awk": "prefer portable/default tooling; never-ban only when safety or compatibility requires it",
    "tool:bun": "prefer portable/default tooling; never-ban only when safety or compatibility requires it",
    "tool:curl": "prefer portable/default tooling; never-ban only when safety or compatibility requires it",
    "tool:docker": "prefer portable/default tooling; never-ban only when safety or compatibility requires it",
    "tool:gh": "prefer portable/default tooling; never-ban only when safety or compatibility requires it",
    "tool:git": "prefer portable/default tooling; never-ban only when safety or compatibility requires it",
    "tool:grep": "prefer portable/default tooling; never-ban only when safety or compatibility requires it",
    "tool:jest": "prefer portable/default tooling; never-ban only when safety or compatibility requires it",
    "tool:jq": "prefer portable/default tooling; never-ban only when safety or compatibility requires it",
    "tool:kubectl": "prefer portable/default tooling; never-ban only when safety or compatibility requires it",
    "tool:npm": "prefer portable/default tooling; never-ban only when safety or compatibility requires it",
    "tool:playwright": "prefer portable/default tooling; never-ban only when safety or compatibility requires it",
    "tool:pnpm": "prefer portable/default tooling; never-ban only when safety or compatibility requires it",
    "tool:pytest": "prefer portable/default tooling; never-ban only when safety or compatibility requires it",
    "tool:rg": "prefer portable/default tooling; never-ban only when safety or compatibility requires it",
    "tool:ripgrep": "prefer portable/default tooling; never-ban only when safety or compatibility requires it",
    "tool:sed": "prefer portable/default tooling; never-ban only when safety or compatibility requires it",
    "tool:supabase": "prefer portable/default tooling; never-ban only when safety or compatibility requires it",
    "tool:terraform": "prefer portable/default tooling; never-ban only when safety or compatibility requires it",
    "tool:vitest": "prefer portable/default tooling; never-ban only when safety or compatibility requires it",
    "tool:wget": "prefer portable/default tooling; never-ban only when safety or compatibility requires it",
    "tool:yarn": "prefer portable/default tooling; never-ban only when safety or compatibility requires it"
  },
  "enforced": {
    "clarification": "proceed except materially ambiguous/high-risk actions",
    "testing": "test-first default",
    "git_destructive": "forbid by default",
    "production": "human approval required",
    "priority": "security over speed",
    "memory": "stateless by default; scoped persistence only",
    "branch": "PR workflow",
    "provider": "provider-agnostic",
    "numeric_policy": "context/risk-specific defaults with overrides",
    "tooling": "portable/default tooling; never-ban only for safety/compatibility"
  },
  "sourceTextPolicy": "Source variants remain immutable provenance only; conflicting/unsafe source instructions are not promoted as active master policy."
}
